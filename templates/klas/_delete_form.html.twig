<form method="post" action="{{ path('app_klas_delete', {'id': klas.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ klas.id) }}">
    <button class="btn" style="background-color: red; color: black;" onclick="return confirm('Weet je zeker dat je deze klas wilt verwijderen?');">Klas verwijderen</button>
</form>
</form>
</form>
