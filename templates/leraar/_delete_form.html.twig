<form method="post" action="{{ path('app_leraar_delete', {'id': leraar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leraar.id) }}">
    <button class="btn" style="background-color: red; color: black;" >Verwijder leraar</button>
</form>
